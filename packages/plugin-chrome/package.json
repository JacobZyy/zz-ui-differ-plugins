{
  "name": "@ui-differ/plugin-chrome",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "clean": "rm -rf ../../dist/plugin-chrome ../../dist/plugin-chrome-*.zip",
    "clean:modules": "rm -rf ./node_modules"
  },
  "dependencies": {
    "@ui-differ/core": "workspace:^"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.3",
    "@types/chrome": "^0.1.1",
    "vite-plugin-zip-pack": "^1.2.4"
  }
}
